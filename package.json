{
  "name": "aws-read-region",
  "description": "Read the AWS region from environment variables or ~/.aws config.",
  "repository": "git://github.com/joshrtay/aws-read-region.git",
  "version": "1.0.3",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish"
  },
  "dependencies": {
    "co": "^4.6.0",
    "ini": "^1.3.4",
    "mz": "^2.4.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
