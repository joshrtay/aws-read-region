{
  "name": "@yaws/read-region",
  "description": "Read the AWS region from environment variables or ~/.aws config.",
  "repository": "git://github.com/yaws/read-region.git",
  "version": "0.1.5",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "ini": "^1.3.4"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "tape": "^4.2.2"
  }
}
